<?xml version="1.0" encoding="UTF-8"?>
<schemalist>
  <schema id="apps.twitch-indicator" path="/apps/twitch-indicator/">
    <key type="b" name="enable-notifications">
      <default>true</default>
      <summary>Enable live stream notifications</summary>
      <description>When followed channel goes live, will show a notification.</description>
    </key>

    <key type="b" name="show-game-playing">
      <default>true</default>
      <summary>Show played game</summary>
      <description>Shows the game the streamer is playing in notifications and menu.</description>
    </key>

    <key type="b" name="show-viewer-count">
      <default>true</default>
      <summary>Show viewer count</summary>
      <description>Shows viewer count in notifications and menu.</description>
    </key>

    <key type="b" name="show-selected-channels-on-top">
      <default>true</default>
      <summary>Sort selected channels to top</summary>
      <description>Shows selected channels on top in the live channels list.</description>
    </key>

    <key type="s" name="open-command">
      <default>"{browser} {url}"</default>
      <summary>Open Command</summary>
      <description>Which command is used to open the stream. {browser} and {url} are available as the default browser and the url of the stream.</description>
    </key>

    <key type="i" name="refresh-interval">
      <default>5</default>
      <summary>Refresh interval.</summary>
      <description>How many minutes should indicator wait between refreshing your followed channels.</description>
    </key>

    <key type="s" name="enabled-channel-ids">
      <default>""</default>
      <summary>Enabled channels.</summary>
      <description>Channels for which notifications are enabled.</description>
    </key>
  </schema>
</schemalist>
